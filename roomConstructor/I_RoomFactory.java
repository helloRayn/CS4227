package roomConstructor;

public interface I_RoomFactory {
	//Interface for Abstract Factory method
	
	//Main method to be called.
	public Room buildRoom();
	
	//May add another method if we start giving an input
}
